import { AppointmentViewmodel } from './appointment-viewmodel';

describe('AppointmentViewmodel', () => {
  it('should create an instance', () => {
    expect(new AppointmentViewmodel()).toBeTruthy();
  });
});
