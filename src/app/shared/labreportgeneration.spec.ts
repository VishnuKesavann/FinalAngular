import { Labreportgeneration } from './labreportgeneration';

describe('Labreportgeneration', () => {
  it('should create an instance', () => {
    expect(new Labreportgeneration()).toBeTruthy();
  });
});
