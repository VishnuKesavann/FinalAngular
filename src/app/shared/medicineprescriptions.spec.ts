import { Medicineprescriptions } from './medicineprescriptions';

describe('Medicineprescriptions', () => {
  it('should create an instance', () => {
    expect(new Medicineprescriptions()).toBeTruthy();
  });
});
